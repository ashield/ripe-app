<div class="content">
	<div class="projects">
	{{#each projects}}
		<figure>
		   <div class="info">
		       <header class="name">{{name}}</header>
		       <p class="description">{{description}}</p>
		       {{#each _tasks}}
		       <p class="tasks">{{this}}</p>
		       {{/each}}
		    </div>
			<a href="/projects/{{id}}">edit</a>
		</figure>
	{{/each}}
		<figure>
		   <div class="info">
		       <a href="#">+</a>
		   </div>
		</figure>
	</div>
</div>


